# client/app

Статус: целевой модуль, перенос реализации ещё не выполнен.

Файл реализации: `src/bin/client/app.rs`.

## Ответственность

Запуск платформенного клиента и соединение его частей.

## Контракт

[client/app](../../../../contracts/target/modules/client/app.md) — операции, входы/выходы,
гарантии, ошибки и связи с потребителями. Статус: целевой, не реализован.
API определён в контракте; эта карточка описывает архитектурную ответственность.

## Данные и владение

Окно и жизненный цикл клиента.

## Разрешённые зависимости

Все клиентские модули через их API; engine/protocol.

## Граница ответственности

Не выбирает следующую главу кампании и не определяет каталог игровых приключений. Его содержимое приходит из пакета.

## Откуда переносить

src/bin/client.rs; screens/main_menu.rs; screens/adventures.rs

## Проверка при переносе

После смены пакета меняются игровые меню и правила без ветвления по package id в клиенте.

При создании файла реализации перенести разделы «Ответственность», «Данные и владение»
и «Граница ответственности» в его модульный комментарий: `//!` для Rust,
`--` для Lua. Добавить ссылку на контракт выше. Синхронно обновить эту карточку и контракт.
