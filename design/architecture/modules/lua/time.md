# lua/time

Статус: целевой модуль, перенос реализации ещё не выполнен.

Файл реализации: `scripts/game/rules/time.lua`.

## Ответственность

Игровой календарь и влияние времени суток.

## Контракт

[lua/time](../../../../contracts/target/modules/lua/time.md) — операции, входы/выходы,
гарантии, ошибки и связи с потребителями. Статус: целевой, не реализован.
API определён в контракте; эта карточка описывает архитектурную ответственность.

## Данные и владение

Цикл суток и правила модификаторов; текущая фаза в store.

## Разрешённые зависимости

units для чтения способностей освещения.

## Граница ответственности

Не выбирает UI-картинку и не перекрашивает карту; presentation использует рассчитанную фазу.

## Откуда переносить

phase/alignment_modifier/illuminated_lawful_bonus в basic_combat.lua

## Проверка при переносе

Смена фазы одинаково влияет на прогноз и бой; порядок advance задаёт turns.

При создании файла реализации перенести разделы «Ответственность», «Данные и владение»
и «Граница ответственности» в его модульный комментарий: `//!` для Rust,
`--` для Lua. Добавить ссылку на контракт выше. Синхронно обновить эту карточку и контракт.
