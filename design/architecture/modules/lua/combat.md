# lua/combat

Статус: целевой модуль, перенос реализации ещё не выполнен.

Файл реализации: `scripts/game/rules/combat.lua`.

## Ответственность

Разрешение боя и общие для выполнения/прогноза боевые расчёты.

## Контракт

[lua/combat](../../../../contracts/target/modules/lua/combat.md) — операции, входы/выходы,
гарантии, ошибки и связи с потребителями. Статус: целевой, не реализован.
API определён в контракте; эта карточка описывает архитектурную ответственность.

## Данные и владение

Боевые формулы и порядок ударов; записи результата через tx, случайность через tx RNG.

## Разрешённые зависимости

units, sides, terrain, time.

## Граница ответственности

Не рисует анимации и не завершает сценарий. Смерть и попадания возвращает в flow. Не зависит от forecast.

## Откуда переносить

basic_combat prepare_battle/strike/resolve; src/combat.rs impact

## Проверка при переносе

Фиксированная последовательность RNG воспроизводит бой; сценарные последствия выполняются flow.

При создании файла реализации перенести разделы «Ответственность», «Данные и владение»
и «Граница ответственности» в его модульный комментарий: `//!` для Rust,
`--` для Lua. Добавить ссылку на контракт выше. Синхронно обновить эту карточку и контракт.
