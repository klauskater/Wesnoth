# lua/forecast

Статус: целевой модуль, перенос реализации ещё не выполнен.

Файл реализации: `scripts/game/rules/forecast.lua`.

## Ответственность

Прогноз вероятностей исходов боя без изменения мира.

## Контракт

[lua/forecast](../../../../contracts/target/modules/lua/forecast.md) — операции, входы/выходы,
гарантии, ошибки и связи с потребителями. Статус: целевой, не реализован.
API определён в контракте; эта карточка описывает архитектурную ответственность.

## Данные и владение

Временные ветви вероятностей, без авторитетного состояния.

## Разрешённые зависимости

combat: тот же чистый step и подготовленные параметры.

## Граница ответственности

Не потребляет игровой RNG. Rust combat kernel с slow/petrify переносится сюда; новый универсальный native kernel заранее не проектируется.

## Откуда переносить

src/combat.rs forecast; basic_combat.preview_attack

## Проверка при переносе

Прогноз и исполнение совпадают на детерминированных случаях и случаях со статусами; измерены время и размер кеша.

При создании файла реализации перенести разделы «Ответственность», «Данные и владение»
и «Граница ответственности» в его модульный комментарий: `//!` для Rust,
`--` для Lua. Добавить ссылку на контракт выше. Синхронно обновить эту карточку и контракт.
