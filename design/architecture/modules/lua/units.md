# lua/units

Статус: целевой модуль, перенос реализации ещё не выполнен.

Файл реализации: `scripts/game/rules/units.lua`.

## Ответственность

Создание и изменение юнитов по каталогам, traits и развитию.

## Контракт

[lua/units](../../../../contracts/target/modules/lua/units.md) — операции, входы/выходы,
гарантии, ошибки и связи с потребителями. Статус: целевой, не реализован.
API определён в контракте; эта карточка описывает архитектурную ответственность.

## Данные и владение

Схема свойств юнита и преобразований типа; сами записи находятся в store.

## Разрешённые зависимости

Каталоги и базовый ctx; без flow/combat/presentation.

## Граница ответственности

Не решает, сколько XP даётся за конкретное событие боя: это передаёт flow по правилам игры; не управляет UI выбора.

## Откуда переносить

basic_combat apply_trait_effect/apply_advancement/advance_unit; загрузка unit types из Rust

## Проверка при переносе

Развитие сохраняет положенные свойства, переносит XP и создаёт ожидаемый выбор.

При создании файла реализации перенести разделы «Ответственность», «Данные и владение»
и «Граница ответственности» в его модульный комментарий: `//!` для Rust,
`--` для Lua. Добавить ссылку на контракт выше. Синхронно обновить эту карточку и контракт.
