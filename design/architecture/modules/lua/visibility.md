# lua/visibility

Статус: целевой модуль, перенос реализации ещё не выполнен.

Файл реализации: `scripts/game/rules/visibility.lua`.

## Ответственность

Обзор, туман, исследованность и скрытые объекты.

## Контракт

[lua/visibility](../../../../contracts/target/modules/lua/visibility.md) — операции, входы/выходы,
гарантии, ошибки и связи с потребителями. Статус: целевой, не реализован.
API определён в контракте; эта карточка описывает архитектурную ответственность.

## Данные и владение

История исследования в store; вычисляемый обзор — кеш по ревизии и наблюдателю.

## Разрешённые зависимости

movement, sides, units, terrain.

## Граница ответственности

present только читает; исследование обновляется командой. Данные скрытых объектов не попадают в scene, UI, hit ids или effects.

## Откуда переносить

visibility/hidden_from/update_shroud/can_see в basic_combat.lua

## Проверка при переносе

Смена наблюдателя не использует чужой кеш; скрытый враг отсутствует во всём клиентском пакете.

При создании файла реализации перенести разделы «Ответственность», «Данные и владение»
и «Граница ответственности» в его модульный комментарий: `//!` для Rust,
`--` для Lua. Добавить ссылку на контракт выше. Синхронно обновить эту карточку и контракт.
