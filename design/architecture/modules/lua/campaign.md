# lua/campaign

Статус: целевой модуль, перенос реализации ещё не выполнен.

Файл реализации: `scripts/game/campaign.lua`.

## Ответственность

Каталог приключений и правила перехода между сценариями.

## Контракт

[lua/campaign](../../../../contracts/target/modules/lua/campaign.md) — операции, входы/выходы,
гарантии, ошибки и связи с потребителями. Статус: целевой, не реализован.
API определён в контракте; эта карточка описывает архитектурную ответственность.

## Данные и владение

Прогресс кампании, переносимые units/gold/variables в store.

## Разрешённые зависимости

rules/units, sides; декларативные ресурсы.

## Граница ответственности

Не читает файловую систему напрямую и не открывает клиентские экраны. Загрузка следующей главы проходит bootstrap через flow.

## Откуда переносить

src/adventure.rs; CampaignState/campaign_state/next_scenario в src/game.rs

## Проверка при переносе

Переход переносит ровно разрешённые данные; загрузка сохранения не повторяет перенос.

При создании файла реализации перенести разделы «Ответственность», «Данные и владение»
и «Граница ответственности» в его модульный комментарий: `//!` для Rust,
`--` для Lua. Добавить ссылку на контракт выше. Синхронно обновить эту карточку и контракт.
