# lua/ai

Статус: целевой модуль, перенос реализации ещё не выполнен.

Файл реализации: `scripts/game/ai.lua`.

## Ответственность

Выбор допустимых команд для компьютерной стороны.

## Контракт

[lua/ai](../../../../contracts/target/modules/lua/ai.md) — операции, входы/выходы,
гарантии, ошибки и связи с потребителями. Статус: целевой, не реализован.
API определён в контракте; эта карточка описывает архитектурную ответственность.

## Данные и владение

Эвристики и сбрасываемые кеши; продолжительный план, если нужен для сохранений, хранится в store.

## Разрешённые зависимости

rules/movement, combat, forecast, economy, visibility, sides.

## Граница ответственности

Не пишет мир в обход dispatch. Использует разрешённое стороне знание. Не исполняет весь ход неограниченным блокирующим циклом.

## Откуда переносить

scripts/rules/simple_ai.lua

## Проверка при переносе

Команды AI проходят те же проверки, что человеческие; планирование ограничено бюджетом и продолжимо.

При создании файла реализации перенести разделы «Ответственность», «Данные и владение»
и «Граница ответственности» в его модульный комментарий: `//!` для Rust,
`--` для Lua. Добавить ссылку на контракт выше. Синхронно обновить эту карточку и контракт.
