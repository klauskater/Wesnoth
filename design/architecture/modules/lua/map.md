# lua/map

Статус: целевой модуль, перенос реализации ещё не выполнен.

Файл реализации: `scripts/game/presentation/map.lua`.

## Ответственность

Подготовка всей видимой сцены гекс-поля.

## Контракт

[lua/map](../../../../contracts/target/modules/lua/map.md) — операции, входы/выходы,
гарантии, ошибки и связи с потребителями. Статус: целевой, не реализован.
API определён в контракте; эта карточка описывает архитектурную ответственность.

## Данные и владение

Визуальные соглашения игры; стабильные ids объектов сцены и кеши участков.

## Разрешённые зависимости

terrain семейства, rules/visibility, time, movement, units.

## Граница ответственности

Не выполняет GPU-вызовы. Не передаёт скрытые игровые данные для последующей фильтрации клиентом.

## Откуда переносить

src/map.rs/terrain_rules.rs игровая часть; village_renderer.rs; unit drawing screens/game.rs

## Проверка при переносе

Сцена полностью определяет отображение и обновляется без запросов к Lua на кадр.

При создании файла реализации перенести разделы «Ответственность», «Данные и владение»
и «Граница ответственности» в его модульный комментарий: `//!` для Rust,
`--` для Lua. Добавить ссылку на контракт выше. Синхронно обновить эту карточку и контракт.
