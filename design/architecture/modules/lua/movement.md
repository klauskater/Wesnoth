# lua/movement

Статус: целевой модуль, перенос реализации ещё не выполнен.

Файл реализации: `scripts/game/rules/movement.lua`.

## Ответственность

Правила перемещения и достижимости.

## Контракт

[lua/movement](../../../../contracts/target/modules/lua/movement.md) — операции, входы/выходы,
гарантии, ошибки и связи с потребителями. Статус: целевой, не реализован.
API определён в контракте; эта карточка описывает архитектурную ответственность.

## Данные и владение

Правила MP, занятости и ZOC; записи позиции и MP через tx.

## Разрешённые зависимости

sides, terrain, units; host.pathfinding.

## Граница ответственности

Не вызывает боевой resolve и не запускает сценарные события сам; возвращает факты перемещения в flow.

## Откуда переносить

basic_combat.reachable/move; игровая часть src/pathfinding.rs

## Проверка при переносе

Предпросмотр и выполнение используют одинаковые ограничения; вход в ZOC завершает движение по правилам.

При создании файла реализации перенести разделы «Ответственность», «Данные и владение»
и «Граница ответственности» в его модульный комментарий: `//!` для Rust,
`--` для Lua. Добавить ссылку на контракт выше. Синхронно обновить эту карточку и контракт.
