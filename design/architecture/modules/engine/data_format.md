# engine/data_format

Статус: реализован.

Файл реализации: `src/engine/data_format.rs`.

## Ответственность

Синтаксический разбор декларативных ресурсов.

## Контракт

[engine/data_format](../../../../contracts/target/modules/engine/data_format.md) — операции, входы/выходы,
гарантии, ошибки и связи с потребителями. Статус: целевой, не реализован.
API определён в контракте; эта карточка описывает архитектурную ответственность.

## Данные и владение

Только формат дерева; семантика тегов принадлежит Lua.

## Разрешённые зависимости

value.

## Граница ответственности

Не требует [unit], [scenario] или [race]. Не применяет игровые значения по умолчанию.

## Откуда переносить

src/wml.rs

## Проверка при переносе

Неизвестный игре тег можно синтаксически разобрать; ошибочная вложенность диагностируется.

При создании файла реализации перенести разделы «Ответственность», «Данные и владение»
и «Граница ответственности» в его модульный комментарий: `//!` для Rust,
`--` для Lua. Добавить ссылку на контракт выше. Синхронно обновить эту карточку и контракт.
