# engine/hex

Статус: реализован.

Файл реализации: `src/engine/hex.rs`.

## Ответственность

Геометрия гексагональной сетки.

## Контракт

[engine/hex](../../../../contracts/target/modules/engine/hex.md) — операции, входы/выходы,
гарантии, ошибки и связи с потребителями. Статус: целевой, не реализован.
API определён в контракте; эта карточка описывает архитектурную ответственность.

## Данные и владение

Геометрические типы и конверсия координат, без игровых свойств ячеек.

## Разрешённые зависимости

Нет игровых зависимостей.

## Граница ответственности

Не превращает код terrain в village/forest и не задаёт стоимость хода.

## Откуда переносить

Map/Position в src/engine.rs; геометрические helpers Lua

## Проверка при переносе

Соседство и расстояния согласованы на краях и для чётных/нечётных столбцов.

При создании файла реализации перенести разделы «Ответственность», «Данные и владение»
и «Граница ответственности» в его модульный комментарий: `//!` для Rust,
`--` для Lua. Добавить ссылку на контракт выше. Синхронно обновить эту карточку и контракт.
