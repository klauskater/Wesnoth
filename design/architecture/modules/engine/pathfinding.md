# engine/pathfinding

Статус: реализован; универсальный пакетный поиск не знает terrain, MP, ZOC и занятость.

Файл реализации: `src/engine/pathfinding.rs`.

## Ответственность

Пакетный поиск по заданному графу гексов.

## Контракт

[engine/pathfinding](../../../../contracts/target/modules/engine/pathfinding.md) — операции, входы/выходы,
гарантии, ошибки и связи с потребителями. Статус: реализован.
API определён в контракте; эта карточка описывает архитектурную ответственность.

## Данные и владение

Только временные структуры алгоритма.

## Разрешённые зависимости

hex.

## Граница ответственности

Не знает MP, ZOC, союзов и terrain. Не вызывает Lua для каждого посещённого гекса; все веса и ограничения переданы пакетом.

## Откуда переносить

src/pathfinding.rs

## Проверка при переносе

Разные наборы весов дают разные маршруты без изменения Rust; число callback на ячейку равно нулю.

При создании файла реализации перенести разделы «Ответственность», «Данные и владение»
и «Граница ответственности» в его модульный комментарий: `//!` для Rust,
`--` для Lua. Добавить ссылку на контракт выше. Синхронно обновить эту карточку и контракт.
